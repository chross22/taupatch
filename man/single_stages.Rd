% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zoop_data.R
\name{single_stages}
\alias{single_stages}
\title{Individually resolved life stages}
\usage{
single_stages()
}
\value{
character vector of single-stage codes
}
\description{
The copepodite stages plus the adult stage. Selecting these is unambiguous:
they never overlap, so summing any subset counts each animal once.
}
\details{
The database also holds combination columns spanning several stages
(\code{cfin_CV_VI}, \code{pseudo_CI_IV}, \code{ctyp_IV_VI}, and the unstaged \code{ctyp_C}).
Those are deliberately not offered for selection, because they overlap the
single stages — summing \code{CV} together with \code{CV_VI} would count the same
animals twice.
}
