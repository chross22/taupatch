% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_covariate_heatmap}
\alias{plot_covariate_heatmap}
\title{Plot a month-by-year heatmap of a covariate}
\usage{
plot_covariate_heatmap(means, covariate = NULL, path = NULL)
}
\arguments{
\item{means}{a data frame from \code{covariate_monthly_means()}}

\item{covariate}{which covariate to plot; defaults to the first present}

\item{path}{where to write a PNG; \code{NULL} returns the plot instead}
}
\value{
the plot object, or \code{path} invisibly when written to disk
}
\description{
Shows the study-area mean of one covariate for every month and year, which
makes the seasonal cycle read down each column and interannual change read
across rows. Useful for spotting gaps in the covariate record and for sanity-
checking that a fetched variable behaves the way it should (SST peaking in
late summer, for instance).
}
\examples{
\dontrun{
result <- run_taupatch("inst/configs/mock_test.yaml")
plot_covariate_heatmap(result$covariate_means, "SST")
}
}
