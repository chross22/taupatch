% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raw_data.R
\name{split_dates}
\alias{split_dates}
\title{Split a date column into year, month, and day}
\usage{
split_dates(
  dat,
  column = "DATE",
  formats = c("\%d-\%b-\%y", "\%d-\%b-\%Y", "\%Y-\%m-\%d", "\%m/\%d/\%Y", "\%m/\%d/\%y"),
  keep = FALSE
)
}
\arguments{
\item{dat}{a data frame with a date column}

\item{column}{name of the date column}

\item{formats}{candidate \code{strptime} formats, tried in order; the first that
parses every non-missing value is used. The default is the ECOMON export's
\code{05-JAN-03}, followed by the usual four-digit and slash-separated variants.}

\item{keep}{whether to keep the original date column}
}
\value{
\code{dat} with integer \code{year}, \code{month}, and \code{day} columns added
}
\description{
The raw ECOMON export carries one \code{DATE} column, while the rest of this
package works in separate \code{year}, \code{month}, and \code{day} columns — that is what
\code{columns.year}/\code{month}/\code{day} name, and what the covariate matching joins on.
This is the conversion, taken from \code{original/create_database.R:27-29}.
}
\details{
Two things the original left implicit are handled here, because both fail
quietly rather than loudly:
\itemize{
\item \verb{\%b} matches an abbreviated month \emph{name}, which is locale-dependent. Under a
non-English \code{LC_TIME} every \code{05-JAN-03} parses to \code{NA} and the rows are
silently dropped later as incomplete. Parsing runs under the C locale.
\item \verb{\%y} is a two-digit year, which R maps 00–68 to the 2000s and 69–99 to the
1900s. That is correct for an ECOMON record running 1977 to the present, but
it means a date can only be read as within roughly the last century — so a
parsed date in the future is reported rather than accepted.
}
}
\examples{
raw <- data.frame(STATION = 1:3, DATE = c("05-JAN-03", "17-JUN-11", "28-SEP-19"))
split_dates(raw)
}
