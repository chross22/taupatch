% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_covariate_seasonal}
\alias{plot_covariate_seasonal}
\title{Plot a covariate's seasonal cycle, one line per year}
\usage{
plot_covariate_seasonal(means, covariate = NULL, path = NULL)
}
\arguments{
\item{means}{a data frame from \code{\link[=covariate_monthly_means]{covariate_monthly_means()}}}

\item{covariate}{which covariate to plot; defaults to the first}

\item{path}{optional file to write the plot to instead of returning it}
}
\value{
a \code{ggplot} object, or \code{path} invisibly when writing
}
\description{
The same values the heatmap shows, read the other way: a heatmap is good at
where the record has gaps and poor at how large a departure is, because the
eye cannot compare two colours as precisely as two heights. A year running
warm shows here as a line sitting above the others.
}
\examples{
\dontrun{
plot_covariate_seasonal(result$covariate_means, "SST")
}
}
