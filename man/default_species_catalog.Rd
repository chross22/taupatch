% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{default_species_catalog}
\alias{default_species_catalog}
\title{Default species catalog}
\usage{
default_species_catalog()
}
\value{
a named list suitable for \code{species.catalog}
}
\description{
The three taxa the original zooplankton database resolves, each thresholded at
the 90th percentile as in Ross et al. (2023).
}
\details{
Each entry uses \code{column_prefix} with no \code{stages}, meaning "sum every life
stage" — equivalent to the \verb{<species>_total} column, but letting a run narrow
to particular stages without changing the catalog. \code{pcal} is the one species
whose preferred name differs from its database prefix (\verb{pseudo_*}), so it
declares the alias explicitly.
}
