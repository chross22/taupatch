% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raw_data.R
\name{raw_abundance_suffix}
\alias{raw_abundance_suffix}
\title{The abundance suffix a raw export uses}
\usage{
raw_abundance_suffix(header)
}
\arguments{
\item{header}{the file's column names}
}
\value{
the suffix, including its leading underscore; \code{"_10M2"} when the
header has none, since that is what the ECOMON export uses
}
\description{
Read off the header rather than assumed, so a file counting per 100 cubic
metres is handled without being told. The most common suffix wins when a file
carries more than one, which \code{warn_mixed_units()} reports separately.
}
\examples{
raw_abundance_suffix(c("STATION", "CALANUS_FINMARCHICUS_100M3"))
}
