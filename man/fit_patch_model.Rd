% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{fit_patch_model}
\alias{fit_patch_model}
\title{Fit a patch habitat suitability model}
\usage{
fit_patch_model(dat, config)
}
\arguments{
\item{dat}{labeled modeling data from \code{label_patch()} with covariates attached}

\item{config}{a config list, as returned by \code{load_config()}}
}
\value{
a list with \code{workflow} (the fitted workflow), \code{metrics} (cross-validated
performance), \code{importance} (permutation variable importance), \code{predictors},
and \code{threshold}
}
\description{
Fits a tidymodels workflow classifying stations as inside or outside a
high-abundance patch. Replaces the \code{biomod2} model in
\code{original/buildZoopModel.R}: cross-validation via \code{rsample::vfold_cv()} instead
of biomod2's \code{CV.nb.rep}/\code{data.split.perc}, and a \code{workflows::workflow()} object
instead of biomod2's on-disk model directory and string-pasted run names.
}
