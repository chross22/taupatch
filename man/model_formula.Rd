% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_types.R
\name{model_formula}
\alias{model_formula}
\title{Formula for a model type that needs one}
\usage{
model_formula(type, model_data, predictors, config = NULL)
}
\arguments{
\item{type}{a model type name}

\item{model_data}{the data the model will be fitted on}

\item{predictors}{predictor column names}
}
\value{
a formula, or \code{NULL} for types that do not need one
}
\description{
\code{mgcv} has to be told which terms are smooth, and \code{parsnip} passes that as a
formula rather than as arguments. Every predictor gets a smooth except those
with too few distinct values to support one: a thin-plate spline needs more
unique points than it has basis functions, and \code{s()} on a near-constant column
fails rather than degrading. Those enter linearly, which is what a smooth
would have reduced to anyway.
}
\keyword{internal}
