% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bathymetry.R
\name{fetch_bathymetry}
\alias{fetch_bathymetry}
\title{Fetch static bathymetry covariates for the study area}
\usage{
fetch_bathymetry(config, resolution = 4, keep = TRUE)
}
\arguments{
\item{config}{a config list, as returned by \code{load_config()}}

\item{resolution}{grid resolution in arc-minutes, as \code{marmap} defines it;
smaller is finer and slower. 4 is roughly 7 km at these latitudes.}

\item{keep}{whether \code{marmap} should cache the downloaded grid for reuse}
}
\value{
a \code{terra::SpatRaster} with one layer per covariate in
\code{bathymetry_covariates()}, in EPSG:4326
}
\description{
Downloads NOAA ETOPO bathymetry via \code{marmap::getNOAA.bathy()} and derives slope
and aspect from it.
}
\details{
\code{marmap} caches downloads next to the working directory by default; this passes
\code{path} so the cache lands in the config's output directory instead, keeping a
run's downloads with the rest of its outputs.
}
