% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{save_config}
\alias{save_config}
\title{Write a config list to a YAML file}
\usage{
save_config(config, path, header = TRUE)
}
\arguments{
\item{config}{a config list}

\item{path}{where to write it}

\item{header}{whether to lead the file with orientation comments}
}
\value{
\code{path}, invisibly
}
\description{
The counterpart to \code{\link[=load_config]{load_config()}}, and what both \code{\link[=generate_config]{generate_config()}} and the
app's download button use, so a config saved from a session and one written
programmatically are the same file.
}
\details{
\code{species$resolved} is dropped. \code{load_config()} derives it on the way in, and
writing it back would put a computed field in a file meant to be edited by
hand — and one that silently overrides the catalog it was derived from.
}
\examples{
\dontrun{
config <- load_config("my_run.yaml")
config$model$trees <- 1000
save_config(config, "my_run_1000.yaml")
}
}
