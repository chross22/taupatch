% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transforms.R
\name{covariate_transforms}
\alias{covariate_transforms}
\title{Covariate transformations available in the recipe}
\usage{
covariate_transforms()
}
\value{
a named list, one entry per transform, each with \code{label},
\code{description}, \code{folds_sign}, \code{undefined_at_zero}, and either \code{fn} (applied
directly) or \code{step} (a \code{recipes} step function)
}
\description{
Which transformation a covariate needs is a modeling choice, so it is config
rather than code. The original applied \code{log(abs(x))} to a hardcoded trio of
chlorophyll, integrated chlorophyll, and bathymetry
(\code{original/buildZoopModel.R:134-136}).
}
\details{
Two properties decide what a transform can safely be given:
\itemize{
\item \code{folds_sign} marks the transforms that take \code{abs(x)} first, because they are
undefined for negative input. That is right for a magnitude stored with a
sign convention — bathymetry is negative depth — and wrong for a genuinely
signed covariate, where it maps \code{-2} and \code{2} onto the same value. Derived
covariates make the second case common: a temporal gradient, a vertical
gradient, and the current components are all signed. A transform warns when
the column it is given actually holds both signs.
\item \code{undefined_at_zero} marks \code{log} and \code{log10}, which return \code{-Inf} there.
Zeros are ordinary in chlorophyll and in any derived integral that starts at
zero, so this is checked against the data rather than trusted.
}

\code{boxcox} and \code{yeojohnson} estimate their parameter from the data instead of
applying a fixed function, so they are recipe steps rather than a function of
\code{x}. Box-Cox requires strictly positive input; Yeo-Johnson does not, which
makes it the one estimated option that suits a signed covariate.
}
\examples{
names(covariate_transforms())
covariate_transforms()$log1p$description
}
