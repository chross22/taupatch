% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raw_data.R
\name{format_zoop_data}
\alias{format_zoop_data}
\title{Build a station database from a raw zooplankton export}
\usage{
format_zoop_data(
  path,
  suffix = "_10M2",
  stage_pattern = stage_suffix_pattern(),
  date_column = "DATE",
  longitude = "LONGITUDE",
  latitude = "LATITUDE",
  write_to = NULL
)
}
\arguments{
\item{path}{path to the raw CSV, or a data frame already read}

\item{suffix}{the units suffix marking taxon columns}

\item{stage_pattern}{regular expression matching a life stage; see \code{\link[=zoop_taxa]{zoop_taxa()}}}

\item{date_column}{the column holding the sampling date}

\item{longitude, latitude}{the coordinate columns}

\item{write_to}{optional path to write the result to as CSV}
}
\value{
a tibble with \code{station}, \code{year}, \code{month}, \code{day}, \code{lon}, \code{lat}, one
column per taxon or taxon-stage, the original date column, and every other
column the file carried
}
\description{
The raw export is not the shape the rest of this package reads. It carries one
\code{DATE} rather than year, month and day; \code{LATITUDE}/\code{LONGITUDE} rather than
\code{lat}/\code{lon}; and taxon columns named for their units. This is the conversion,
generalising what \code{original/create_database.R} does once per taxon by hand.
}
\section{Life stages}{

Some datasets resolve them and some do not, so this reads which off the file
rather than assuming. A taxon column carrying a stage keeps it —
\code{CALANUS_FINMARCHICUS_CV} — which is the form \code{column_prefix} and \code{stages}
match in a config. A taxon column without one is that taxon's total and keeps
the bare name, which a config reaches through \code{abundance_column}.
\code{\link[=species_catalog_from]{species_catalog_from()}} picks the right one per taxon.
}

\section{Units}{

The suffix is a count and a unit, not just a unit: \verb{_10M2} is animals per ten
square metres. The values are divided through by the number, so what comes out
is per one of whatever remains - per square metre for the ECOMON export. The
unit is recorded on the result as an \code{abundance_unit} attribute rather than
kept in the column names, which are then the names of animals and nothing
else.

This changes the numbers. An absolute threshold written against the raw
counts will be ten times too large once they are per square metre; a
percentile threshold is unaffected, since dividing every value by the same
number does not move a quantile.
}

\section{What it keeps}{

Everything. Taxon columns are renamed, the date and coordinate columns are
converted, and every other column in the file is carried through untouched.
That includes the in-situ measurements the export already holds:
\code{STATION_DEPTH}, \code{SFC_SALT}, \code{BTM_TEMP} and the rest. They are not covariates.
Nothing in the covariate catalog refers to them, and a run models the gridded
Copernicus fields instead. They are kept because discarding measured values at
the import step is not this function's decision to make.

Note that the export misspells surface temperature as \code{SFC_TMEP}. It is
carried through as found, since silently correcting a column name would make
the output disagree with the file it came from.
}

\examples{
raw <- data.frame(
  STATION = 1:2, DATE = c("05-JAN-03", "17-JUN-11"),
  LATITUDE = c(42.1, 43.2), LONGITUDE = c(-70.1, -69.2),
  STATION_DEPTH = c(80, 120),
  CALANUS_FINMARCHICUS_10M2 = c(1200, 340),
  CENTROPAGES_TYPICUS_CV_10M2 = c(12, 40)
)
format_zoop_data(raw)
}
\seealso{
\code{\link[=split_dates]{split_dates()}}, \code{\link[=zoop_taxa]{zoop_taxa()}}, \code{\link[=species_catalog_from]{species_catalog_from()}}
}
