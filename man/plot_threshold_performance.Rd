% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_threshold_performance}
\alias{plot_threshold_performance}
\title{Plot performance across classification thresholds}
\usage{
plot_threshold_performance(predictions, path = NULL)
}
\arguments{
\item{predictions}{the \code{predictions} element of a \code{fit_patch_model()} result}

\item{path}{where to write a PNG; \code{NULL} returns the plot instead}
}
\value{
the plot object, or \code{path} invisibly when written to disk
}
\description{
Sensitivity, specificity, and TSS as the probability cutoff moves from 0 to 1,
with the TSS-maximising cutoff marked.
}
\details{
The metrics table reports sensitivity and specificity at the default 0.5 cut,
which is rarely the right one for imbalanced data — the original pipeline
binarised its projections at a ROC-derived cutoff for exactly this reason. This
shows what the trade-off looks like across the whole range, and where it is
best balanced.
}
