% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{write_suitability_stack}
\alias{write_suitability_stack}
\title{Write the monthly projections as one multi-layer raster}
\usage{
write_suitability_stack(projections, path)
}
\arguments{
\item{projections}{the tibble \code{\link[=project_patch_model]{project_patch_model()}} returns}

\item{path}{where to write the \code{.grd}}
}
\value{
\code{path}, invisibly
}
\description{
The per-month GeoTIFFs are one file each, which is what a GIS wants and
awkward for anything that treats time as a dimension: a decade is a hundred
and eighty files whose only record of which month they are is the filename.
This stacks them into a single raster with one layer per month, named for it.
}
\details{
Built from the files rather than from rasters held in memory. \code{terra} reads a
stack lazily, so a decade of a real grid is streamed from disk to disk instead
of being assembled in one piece first.
}
\section{Why .grd}{

It is \code{raster}'s native format and \code{terra} writes it, it keeps layer names -
which is the whole point here, since the names carry the dates - and it has no
band limit. GeoTIFF can hold the layers but not their names, so a stacked
\code{.tif} would come back as \code{suitability_1} through \code{suitability_180}. Note that
it is two files: a \code{.grd} header and a \code{.gri} of data, and one is no use
without the other.

One wrinkle worth knowing. \code{terra} writes a \code{.grd} header without recording
each layer's range, so \code{terra::minmax()} on the result reports a placeholder
of some 1e38 rather than the 0 to 1 a probability actually spans. The values
are correct - they match the GeoTIFFs cell for cell - and
\code{terra::global(stack, range, na.rm = TRUE)} gives the real range. Only a
reader that trusts the header, such as an automatic colour scale, is misled,
and setting the range yourself is the fix there.
}

\examples{
\dontrun{
write_suitability_stack(result$projections, "suitability.grd")
}
}
