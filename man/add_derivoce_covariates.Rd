% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/derivoce.R
\name{add_derivoce_covariates}
\alias{add_derivoce_covariates}
\title{Add the configured derived covariates to the covariate grid}
\usage{
add_derivoce_covariates(env_dat, config, bathy = NULL)
}
\arguments{
\item{env_dat}{covariate data from \code{\link[=fetch_covariates]{fetch_covariates()}}}

\item{config}{a config list, as returned by \code{load_config()}}

\item{bathy}{optional static bathymetry \code{SpatRaster} from
\code{\link[datamatch:fetch_bathymetry]{datamatch::fetch_bathymetry()}}, needed only by steps reading a depth column}
}
\value{
\code{env_dat} with one column per derived covariate added
}
\description{
Runs each \code{covariates.derivoce} step in order, passing its fields straight
through to the derivoce function it names. Steps see the columns the earlier
ones produced, so they chain: a \code{current_speed} step followed by a
\code{horizontal_gradient} step on \code{speed} reproduces the original pipeline's
\code{uv_grad}.
}
\details{
This runs on the \strong{covariate grid}, before stations are matched to it, because
a gradient or a front is a property of the field and cannot be recovered from
scattered station points. Everything downstream then treats the results as
ordinary covariate columns: \code{\link[=attach_covariates]{attach_covariates()}} matches them to stations,
\code{\link[=covariate_grid]{covariate_grid()}} carries them onto the projection grid, and they are picked
up as predictors automatically. Contrast \code{\link[=add_derived_covariates]{add_derived_covariates()}}, which adds
\code{jday} to a table of observations and needs one date per row rather than a grid.

Three consequences are worth stating plainly, since each one costs data:
\itemize{
\item Lags, integrals, and temporal gradients are undefined for the first time
step(s) of the record, so stations in those months are dropped when covariates
are matched, and the earliest month's projection has nothing to predict on.
\item A step reading a point's surroundings is undefined on the edge of the study
area, where the point has no complete neighbourhood. That border is lost from
both the training stations and the projected maps, so a study area drawn to
just contain the stations will lose the outermost ones.
\item A neighbourhood step computed from a variable that was upsampled from a
coarser product measures the source grid rather than the ocean. Those steps
warn when their input is one of the upsampled variables recorded by
\code{\link[=fetch_covariates]{fetch_covariates()}}.
}
}
\examples{
\dontrun{
env_dat <- fetch_covariates(config)
env_dat <- add_derivoce_covariates(env_dat, config)
}
}
\seealso{
\code{\link[=derivoce_covariates]{derivoce_covariates()}} for the step types and the columns they produce
}
