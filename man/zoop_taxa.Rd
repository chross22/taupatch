% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raw_data.R
\name{zoop_taxa}
\alias{zoop_taxa}
\title{Taxon columns in a raw zooplankton export}
\usage{
zoop_taxa(header, suffix = "_10M2", stage_pattern = stage_suffix_pattern())
}
\arguments{
\item{header}{column names, or a path to a CSV to read them from}

\item{suffix}{the units suffix marking an abundance column}

\item{stage_pattern}{regular expression matching a stage on the end of a
taxon name. The default is a \code{C} followed by a Roman numeral one to six.}
}
\value{
a data frame with one row per taxon column: \code{column} as found,
\code{taxon}, \code{shorthand}, \code{stage} (\code{NA} when the column is a total), \code{name} (the
column the formatted database will carry), and the \code{per}/\code{unit} the counts
are reported in
}
\description{
The export names every taxon column for the taxon and the units it is
reported in: \code{CALANUS_FINMARCHICUS_10M2} is abundance per 10 m². Some datasets
also resolve life stages, marked by a \code{C} and a Roman numeral —
\code{CALANUS_FINMARCHICUS_CV_10M2} is the fifth copepodite stage. A column without
one is the total for that taxon.
}
\details{
Read off the header rather than kept as a list here. The export carries the
better part of a hundred taxa, gains and loses them between versions, and
resolves stages for some datasets and not others. A fixed list would be wrong
shortly after it was written.
}
\examples{
zoop_taxa(c("CALANUS_FINMARCHICUS_10M2", "CALANUS_FINMARCHICUS_CV_10M2"))
}
