% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prejoin.R
\name{prejoin_steps}
\alias{prejoin_steps}
\title{Per-covariate steps applied before products are joined}
\usage{
prejoin_steps()
}
\value{
a named list, one entry per step type, each with \code{label},
\code{description}, \code{targets} (the step fields naming covariates), and \code{fun}
}
\description{
Copernicus products arrive on different grids, and \code{\link[=fetch_covariates]{fetch_covariates()}}
reconciles them by joining everything onto one. That join is all-or-nothing:
every covariate is either upsampled or left alone according to a single
\code{covariates.grid} choice, with no say in how.
}
\details{
A \code{covariates.prejoin} block is that say. Each step names a covariate and
resamples it, or fills its gaps, \emph{before} the join happens — so the join then
sees grids that already agree, and does nothing to them.

The reason to want this is that the right treatment differs per covariate.
Ocean-colour chlorophyll is a 4 km optical retrieval full of cloud gaps;
physics is a 0.083 degree model with no gaps at all. Bringing chlorophyll up to
the physics grid by averaging is a defensible summary of values that were
really measured. Interpolating physics down to 4 km invents structure. One
global setting cannot express that, and the \code{upsampled} warning exists
precisely because the current join cannot either.

The computation is \code{\link[datamatch:upscale_grid]{datamatch::upscale_grid()}}, \code{\link[datamatch:downscale_grid]{datamatch::downscale_grid()}},
and \code{\link[datamatch:fill_satellite_gaps]{datamatch::fill_satellite_gaps()}} throughout, which own it.
}
\examples{
names(prejoin_steps())
prejoin_steps()$upscale$description
}
\seealso{
\code{\link[=apply_prejoin_steps]{apply_prejoin_steps()}}, which runs the configured steps
}
