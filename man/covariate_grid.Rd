% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariates.R
\name{covariate_grid}
\alias{covariate_grid}
\title{Build the covariate grid for one month}
\usage{
covariate_grid(env_dat, year, month, config)
}
\arguments{
\item{env_dat}{covariate data from \code{fetch_covariates()}}

\item{year}{year to extract}

\item{month}{month to extract}

\item{config}{a config list, as returned by \code{load_config()}}
}
\value{
a tibble with \code{lon}, \code{lat}, and one column per predictor, or \code{NULL} if
the covariate data has no points for that year and month
}
\description{
The prediction surface a fitted model is projected onto: every covariate grid
point for one (year, month), with derived covariates added so the grid carries
the same predictors the model was trained on.
}
