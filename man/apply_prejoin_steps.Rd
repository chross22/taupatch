% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prejoin.R
\name{apply_prejoin_steps}
\alias{apply_prejoin_steps}
\title{Apply the configured pre-join steps to the fetched products}
\usage{
apply_prejoin_steps(per_dataset, config)
}
\arguments{
\item{per_dataset}{a list of \code{sf} POINT objects, one per fetched product}

\item{config}{a config list, as returned by \code{load_config()}}
}
\value{
a list of \code{sf} POINT objects, ready to join
}
\description{
Steps run in order, each against the product object holding the covariate it
names, so a later step sees what an earlier one produced.
}
\details{
A step naming one covariate of a product that carries several splits it out
first. \code{datamatch::upscale_grid()} returns only the columns it was asked to
resample, so regridding chlorophyll in place would silently drop the other
plankton variables sharing its dataset. Splitting means the rest stay on their
own grid and reach the join intact.
}
\seealso{
\code{\link[=prejoin_steps]{prejoin_steps()}} for the step types
}
