% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{load_config}
\alias{load_config}
\title{Load and validate a taupatch run config}
\usage{
load_config(path)
}
\arguments{
\item{path}{path to a config YAML file}
}
\value{
the parsed config list with \code{paths} resolved to absolute paths and a
derived \code{species$resolved} entry describing the active species
}
\description{
Reads a run config YAML (see \verb{inst/configs/}), resolves paths relative to
\code{paths.project_dir}, and validates the parts that are cheap to check before a
run rather than midway through one: the active species exists in the catalog,
each species resolves to an abundance column, thresholds are well-formed, and
the declared columns actually exist in the zooplankton CSV.
}
