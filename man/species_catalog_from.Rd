% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raw_data.R
\name{species_catalog_from}
\alias{species_catalog_from}
\title{A species catalog for the taxa in a raw export}
\usage{
species_catalog_from(
  header,
  suffix = "_10M2",
  stage_pattern = stage_suffix_pattern(),
  threshold = list(type = "percentile", value = 0.9),
  aliases = NULL
)
}
\arguments{
\item{header}{column names, a raw export, or a path to one}

\item{suffix}{the units suffix marking taxon columns}

\item{stage_pattern}{regular expression matching a life stage}

\item{threshold}{the threshold every entry gets}

\item{aliases}{optional named character vector giving short config keys to
taxa, e.g. \code{c(cfin = "CALANUS_FINMARCHICUS")}}
}
\value{
a named list suitable for \code{species.catalog}
}
\description{
Writes the \code{species.catalog} block a config needs, one entry per taxon.
Suitable for passing to \code{\link[=generate_config]{generate_config()}} as \code{species}.
}
\details{
Which form an entry takes depends on the file. A taxon whose stages the
dataset resolves gets \code{column_prefix}, so a run can select stages or leave
them out to sum every one. A taxon reported only as a total gets
\code{abundance_column}, because there is no prefix for anything to match.

A taxon carrying both a total and some stages gets the prefix form, and the
bare total column is then not read: \code{column_prefix} matches only
\verb{<taxon>_<something>}, which is what keeps the total from being summed
alongside the stages that compose it. Worth knowing that summing the resolved
stages need not reproduce the reported total, since a dataset may resolve only
some of them. Use \code{abundance_column} explicitly if the total is what you
want.
}
\examples{
header <- c("CALANUS_FINMARCHICUS_CV_10M2", "CALANUS_FINMARCHICUS_CVI_10M2",
            "CENTROPAGES_TYPICUS_10M2")

# cfin resolves stages here, so it gets a prefix; ctyp is a total.
species_catalog_from(header, aliases = c(cfin = "CALANUS_FINMARCHICUS"))
}
\seealso{
\code{\link[=format_zoop_data]{format_zoop_data()}}, \code{\link[=generate_config]{generate_config()}}
}
