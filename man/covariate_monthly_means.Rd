% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariates.R
\name{covariate_monthly_means}
\alias{covariate_monthly_means}
\title{Average each covariate over the study area, per month and year}
\usage{
covariate_monthly_means(env_dat, vars = NULL)
}
\arguments{
\item{env_dat}{covariate data from \code{fetch_covariates()}}

\item{vars}{covariate names to summarize; \code{NULL} uses all of them}
}
\value{
a data frame with \code{year}, \code{month}, \code{covariate}, and \code{mean} columns
}
\description{
Collapses the covariate grid to one value per (year, month, covariate) — the
spatial mean across the study area. Summarizing during a run rather than
returning the full grid keeps the result small: a multi-decade Copernicus fetch
is millions of grid points, while its summary is a few hundred rows.
}
