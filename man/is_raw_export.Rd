% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raw_data.R
\name{is_raw_export}
\alias{is_raw_export}
\title{Whether a header is a raw export rather than a station database}
\usage{
is_raw_export(header)
}
\arguments{
\item{header}{column names, or a path to a CSV to read them from}
}
\value{
\code{TRUE} when the file looks like a raw export
}
\description{
A raw export names its coordinates \code{LATITUDE}/\code{LONGITUDE} and carries one
\code{DATE}; a station database uses \code{lat}/\code{lon} and separate year, month and day.
The two are told apart by that alone, which is enough to know whether a file
needs \code{\link[=format_zoop_data]{format_zoop_data()}} run over it first.
}
\examples{
is_raw_export(c("STATION", "LATITUDE", "LONGITUDE", "DATE"))
is_raw_export(c("station", "lat", "lon", "year", "month", "day"))
}
