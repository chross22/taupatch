% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zoop_data.R
\name{stage_alias_map}
\alias{stage_alias_map}
\title{Stage columns that are aliased to a single-stage name}
\usage{
stage_alias_map(column_prefix)
}
\arguments{
\item{column_prefix}{the species' column prefix in the database}
}
\value{
named list of \code{list(column=, spans=)}, keyed by the stage name to
present, or \code{NULL} when the species needs no aliases
}
\description{
Some species report a stage under a column name that does not look like a
single stage. \code{cfin_CV_VI} is the adult/late-stage count — it is how ECOMON
reports \emph{C. finmarchicus}, which does not resolve CV from CVI — so it is
offered as \code{adult}, matching how \code{ctyp} and \code{pseudo} name theirs.
}
\details{
\code{spans} records which single stages the column covers, so a selection mixing
an alias with a stage it already contains can be rejected instead of
double-counting.
}
\keyword{internal}
