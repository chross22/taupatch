% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_effects.R
\name{plot_gam_smooths}
\alias{plot_gam_smooths}
\title{Plot a GAM's fitted smooths, with fancygam}
\usage{
plot_gam_smooths(model, vars = NULL, path = NULL)
}
\arguments{
\item{model}{a fitted model from \code{\link[=fit_patch_model]{fit_patch_model()}}}

\item{vars}{which smooths to draw; \code{NULL} uses all of them}

\item{path}{optional file to write the plot to instead of returning it}
}
\value{
a \code{patchwork}/\code{ggplot} object, or \code{path} invisibly when writing
}
\description{
The partial effect of each smooth term on the log-odds scale, with its
standard error band and a rug showing where the data actually is. This is the
exact version of \code{\link[=partial_effects]{partial_effects()}} for a GAM: read out of the fitted model
rather than reconstructed by prediction, so it carries uncertainty, which a
partial dependence curve cannot.
}
\details{
Drawn by \href{https://github.com/chross22/fancygam}{fancygam}, which is a Suggests
— a run without it still gets the generic partial effect curves.
}
\section{Why the axes read in standard deviations}{

The smooths belong to the model, and the model was fitted on the recipe's
output — so \code{x} is whatever the recipe made of the predictor. With the default
\code{covariates.normalize: true} that is standard deviations from the mean, and
the rug is taken from the same baked data so the two line up. Set
\code{covariates.normalize: false} to have these read in the covariate's own units;
it costs a tree model nothing, and a GAM little.
}

\examples{
\dontrun{
plot_gam_smooths(model)
}
}
\seealso{
\code{\link[=gam_smooth_terms]{gam_smooth_terms()}} for the numbers behind these
}
