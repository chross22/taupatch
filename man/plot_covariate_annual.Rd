% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_covariate_annual}
\alias{plot_covariate_annual}
\title{Plot a covariate's annual mean over the record}
\usage{
plot_covariate_annual(means, covariate = NULL, path = NULL)
}
\arguments{
\item{means}{a data frame from \code{\link[=covariate_monthly_means]{covariate_monthly_means()}}}

\item{covariate}{which covariate to plot; defaults to the first}

\item{path}{optional file to write the plot to instead of returning it}
}
\value{
a \code{ggplot} object, or \code{path} invisibly when writing
}
\description{
Collapses each year's months to one value, which is the view that shows drift
across the record rather than the seasonal cycle riding on top of it.
}
\details{
The mean is taken over whichever months the run fetched. That makes the series
comparable between years only when every year covers the same months — which
is the normal case here, since covariates are fetched for a fixed month range,
but is worth knowing before reading a trend into it.
}
\examples{
\dontrun{
plot_covariate_annual(result$covariate_means, "SST")
}
}
