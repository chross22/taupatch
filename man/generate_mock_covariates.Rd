% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mock.R
\name{generate_mock_covariates}
\alias{generate_mock_covariates}
\title{Generate synthetic environmental covariates}
\usage{
generate_mock_covariates(
  config,
  years = NULL,
  months = NULL,
  resolution = 0.25,
  seed = 42
)
}
\arguments{
\item{config}{a config list, as returned by \code{load_config()}}

\item{years}{years to generate}

\item{months}{months to generate}

\item{resolution}{grid spacing in degrees}

\item{seed}{random seed}
}
\value{
an \code{sf} POINT object matching \code{fetch_covariates()}'s contract
}
\description{
Produces the same shape \code{datamatch::accessEnvDat()} returns, so the pipeline
runs identically against mock and Copernicus data. Covariates carry the same
latitudinal and seasonal structure planted in the mock abundances, so the model
has something real to learn.
}
