% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mock.R
\name{generate_mock_zoop_data}
\alias{generate_mock_zoop_data}
\title{Generate a synthetic zooplankton database}
\usage{
generate_mock_zoop_data(config, n_stations = 400, seed = 42)
}
\arguments{
\item{config}{a config list, as returned by \code{load_config()}}

\item{n_stations}{number of station visits to generate per year}

\item{seed}{random seed}
}
\value{
the path written, invisibly
}
\description{
Writes a CSV matching the schema \code{original/create_database.R} produces, so the
pipeline can be exercised end-to-end without the real survey data, which stays
on its owner's machine.
}
\details{
Two details are copied from the real database's structure rather than invented,
because the pipeline's behavior depends on them:
\itemize{
\item Stage columns that a survey does not resolve are filled with \strong{zeros},
not NA (\code{create_database.R} lines 32-55 for ECOMON), so \verb{<species>_total}
collapses to the single stage ECOMON measures.
\item \code{dataset} takes the values \code{ECOMON}, \code{ECOMON_STAGED}, \code{MBON}, \code{CPR}, so
the config's \code{dataset_filter} is actually exercised rather than matching
every row.
}

Abundance is given real spatial and seasonal structure — a temperature-like
gradient plus a summer peak — so a model fit to it should score meaningfully
better than chance. A smoke test that passed on pure noise would not be testing
anything.
}
