% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raw_data.R
\name{available_species}
\alias{available_species}
\title{Species a formatted database could model}
\usage{
available_species(
  zoop_file,
  stage_pattern = stage_suffix_pattern(),
  exclude = measurement_columns()
)
}
\arguments{
\item{zoop_file}{path to a formatted database, or its column names}

\item{stage_pattern}{regular expression matching a life stage}

\item{exclude}{columns to leave out. The default drops the in-situ
measurements the ECOMON export carries, which are numeric columns that are
not taxa; pass \code{character()} to see them.}
}
\value{
a data frame of \code{species} (the column header name), \code{shorthand},
\code{form} (\code{"total"} or \code{"stages"}), and \code{stages}, one row per candidate
}
\description{
The config's species catalog names the taxa a run was set up for. A formatted
export carries every taxon the survey counted, which is most of a hundred, and
any of them can be modelled. This reads the candidates off the file.
}
\details{
A taxon with stage columns is reported once, as a prefix, since that is how a
config selects it and how its stages are summed. Everything else is reported
as a total.
}
\section{What it cannot tell you}{

A formatted database has had the units cut off its taxon columns, so nothing
in the name distinguishes a taxon from the in-situ measurements the export
also carries. \code{STATION_DEPTH} and \code{BTM_TEMP} are candidates here in exactly
the way \code{CALANUS_FINMARCHICUS} is. The list is what could be an abundance
column, not what is one. Run \code{\link[=zoop_taxa]{zoop_taxa()}} against the raw export for the
answer the raw column names still hold.
}

\examples{
available_species(c("station", "year", "month", "day", "lon", "lat",
                    "CALANUS_FINMARCHICUS", "PSEUDOCALANUS_SPP_CIV",
                    "PSEUDOCALANUS_SPP_CV"))
}
\seealso{
\code{\link[=zoop_taxa]{zoop_taxa()}}, \code{\link[=species_catalog_from]{species_catalog_from()}}
}
