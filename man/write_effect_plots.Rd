% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_effects.R
\name{write_effect_plots}
\alias{write_effect_plots}
\title{Model-specific diagnostics for a fitted model}
\usage{
write_effect_plots(model, out)
}
\arguments{
\item{model}{a fitted model from \code{\link[=fit_patch_model]{fit_patch_model()}}}

\item{out}{the run's diagnostics directory}
}
\value{
character vector of paths written, invisibly
}
\description{
Partial effects for every type, since the question "what does this predictor
do" is the same question whichever model answered it. Then the one thing the
chosen model can say that the others cannot: signed coefficients for a GLM,
effective degrees of freedom per smooth for a GAM. A forest and a boosted
ensemble have no such summary — their answer \emph{is} the partial effect curve,
which is why that one is computed for all four rather than only where an
engine happens to report something.
}
\keyword{internal}
