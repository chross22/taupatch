# Single stages covered by each selectable stage

Single stages covered by each selectable stage

## Usage

``` r
stage_spans(column_prefix, stages)
```

## Arguments

- column_prefix:

  the species' column prefix in the database

- stages:

  stage names to expand

## Value

named list mapping each stage to the single stages it covers
