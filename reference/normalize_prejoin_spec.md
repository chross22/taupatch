# Normalize one pre-join step specification

Normalize one pre-join step specification

## Usage

``` r
normalize_prejoin_spec(spec)
```

## Arguments

- spec:

  one entry of `covariates.prejoin`

## Value

the spec as a list with a validated `type`
