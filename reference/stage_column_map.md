# Map selectable stage names to their database columns

Map selectable stage names to their database columns

## Usage

``` r
stage_column_map(header, column_prefix)
```

## Arguments

- header:

  column names of the database

- column_prefix:

  the species' column prefix in the database

## Value

named character vector of column names, keyed by stage name
