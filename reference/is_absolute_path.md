# Check whether a path is absolute

Check whether a path is absolute

## Usage

``` r
is_absolute_path(path)
```

## Arguments

- path:

  a file path string

## Value

`TRUE` if `path` starts with `/`, `~`, or a Windows drive letter
