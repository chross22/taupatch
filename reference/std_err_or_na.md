# Pull one metric's standard error from a collect_metrics() table

Pull one metric's standard error from a collect_metrics() table

## Usage

``` r
std_err_or_na(cv_metrics, metric)
```

## Arguments

- cv_metrics:

  a metrics table

- metric:

  the metric name

## Value

the standard error, or `NA_real_` if absent
