# Validate the model block

The type and its tuning, checked at load rather than after the
covariates have been downloaded and the stations matched.

## Usage

``` r
validate_model(config)
```

## Arguments

- config:

  a parsed config list

## Value

`TRUE` invisibly; errors otherwise
