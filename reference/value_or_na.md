# Pull one metric's mean from a collect_metrics() table

Pull one metric's mean from a collect_metrics() table

## Usage

``` r
value_or_na(cv_metrics, metric)
```

## Arguments

- cv_metrics:

  a metrics table

- metric:

  the metric name

## Value

the mean, or `NA_real_` if absent
