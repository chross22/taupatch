# One run's predictions, restricted to shared stations and put in their order

One run's predictions, restricted to shared stations and put in their
order

## Usage

``` r
align_predictions(predictions, shared)
```

## Arguments

- predictions:

  a run's held-out predictions

- shared:

  the station indices to keep

## Value

the matching rows, in `shared` order
