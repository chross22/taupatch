# The two-sided critical value

The two-sided critical value

## Usage

``` r
critical_t(level, df)
```

## Arguments

- level:

  confidence level

- df:

  degrees of freedom

## Value

the critical `t`, or `NA_real_`
