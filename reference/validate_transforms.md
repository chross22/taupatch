# Validate the transform block

Validate the transform block

## Usage

``` r
validate_transforms(config, available)
```

## Arguments

- config:

  a parsed config list

- available:

  covariate names a transform may name

## Value

`TRUE` invisibly; errors otherwise
