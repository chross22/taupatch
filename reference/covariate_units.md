# Units for a covariate name

Units for a covariate name

## Usage

``` r
covariate_units(covariate)
```

## Arguments

- covariate:

  a covariate name

## Value

the units string, or `""` for covariates not in the catalog
