#' @keywords internal
#'
#' @references
#' The model this implements:
#'
#' Ross CH, Runge JA, Roberts JJ, Brady DC, Tupper B, Record NR (2023).
#' Estimating North Atlantic right whale prey based on *Calanus finmarchicus*
#' thresholds. *Marine Ecology Progress Series* **703**, 1-16.
#' \doi{10.3354/meps14204}
#'
#' Most of what a run produces comes from someone else's data or someone else's
#' method, and the obligation to cite travels with those. Each function's own
#' help page carries the references relevant to it — see
#' [copernicus_covariates()] and [bathymetry_covariates()] for the data sources,
#' [derivoce_covariates()] for the derived ones, [covariate_transforms()],
#' [model_types()], and [permutation_importance()] for the methods. The
#' README's References section collects all of them in one place, and
#' `citation("taupatch")` gives this package's own entry.
"_PACKAGE"

#' @importFrom rlang .data
#' @importFrom utils modifyList
NULL
