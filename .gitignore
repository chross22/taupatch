.Rproj.user
.Rhistory
.RData
.Ruserdata
.DS_Store

# Real zooplankton data and model outputs stay local. The survey data has its
# own sharing terms; outputs are regenerable from a config + a run.
data/
output/

# The pre-rebuild biomod2 pipeline.
original/

# Build artifacts. Vignettes are rendered by R CMD build from the .Rmd, so the
# rendered copies are not the source of anything.
*.Rcheck/
*.tar.gz
inst/doc/
vignettes/*.html
vignettes/*.R

